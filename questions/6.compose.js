/**
 *  Composing multiple functions to create more complex ones
 * Ex 
 * compose(
 *  val => val * val,
 *  val => val * val
 * )(2) = 16
 */

const compose = (...fun) => (initialValue) => {

}
module.exports = compose